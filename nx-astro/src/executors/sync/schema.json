{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxAstroSync",
  "title": "Generate TypeScript types for Content Collections and other Astro features",
  "description": "Runs Astro's sync command to generate TypeScript types using the `astro sync` command",
  "type": "object",
  "properties": {
    "root": {
      "type": "string",
      "description": "Project root path (usually provided by Nx)"
    },
    "config": {
      "type": "string",
      "description": "Path to Astro config file",
      "default": "astro.config.mjs"
    },
    "verbose": {
      "type": "boolean",
      "description": "Enable verbose output",
      "default": false
    },
    "additionalArgs": {
      "type": "array",
      "description": "Additional CLI arguments to pass to Astro",
      "items": {
        "type": "string"
      }
    }
  },
  "required": []
}
